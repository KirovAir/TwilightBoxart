{
  // A checkout needs no configuration at all. DataPath defaults to ./data in Development (gitignored;
  // delete it to start from a cold cache) and the Vite dev server's origins are allowed automatically,
  // both from TwilightSettings.Load - facts about the toolchain rather than choices to make here.
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
